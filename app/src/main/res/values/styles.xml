<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="@style/AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--EditText-->
    <style name="EditTextIO">
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="EditTextInput" parent="EditTextIO">
        <item name="android:inputType">numberDecimal</item>
    </style>
    <style name="EditTextOutput" parent="EditTextIO">
        <item name="android:inputType">text</item>
        <item name="android:gravity">right</item>
        <item name="android:clickable">false</item>
        <item name="android:focusable">false</item>
    </style>

    <!--Button-->
    <style name="Button">
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="ButtonNormal" parent="@style/Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/dark</item>
    </style>
    <style name="ButtonWarning" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint" tools:targetApi="lollipop">@color/warning</item>
    </style>
</resources>
